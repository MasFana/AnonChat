# Core database connection
# MONGODB_URI=mongodb://127.0.0.1:27017/anonchat

# Timing / presence tuning (all optional; defaults shown)
# OWNER_AWAY_GRACE_MS=5000             # ms grace before deleting room when owner absent
# RECENT_USER_WINDOW_MS=15000          # users active if lastSeen within this window
# STALE_USER_PRUNE_MS=15000            # users older than this pruned each presence loop
# PRESENCE_INTERVAL_MS=5000            # how often presence/cleanup runs
# HEARTBEAT_INTERVAL_MS=15000          # SSE ping keep-alive interval
# LAST_SEEN_WRITE_THROTTLE_MS=4000     # minimum ms between lastSeen writes per user

# Snapshot / limits
# MAX_SNAPSHOT_MESSAGES=5000           # max messages returned in initial snapshot/state

# Index / TTL tuning
# USERS_LASTSEEN_TTL=60                # seconds; TTL index safety net for users collection

# (No secret for anon IDs; they use crypto randomness automatically)